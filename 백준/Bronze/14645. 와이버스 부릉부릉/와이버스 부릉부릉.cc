#include <iostream>

using namespace std;

int main(int argc, char* argv[]){
	
	int N, K, tmp = 0;

	cin >> N >> K;

	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < K; j++)
		{
			cin >> tmp;
		}
	}

	cout << "비와이";
	return 0;
}